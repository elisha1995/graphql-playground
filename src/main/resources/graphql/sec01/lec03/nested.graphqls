extend type Query {
    customers2: [Customer2]!
}

type Customer2 {
    id: ID!
    name: String
    age: Int
    city: String
    orders(limit: Int!): [CustomerOrder]!
}

type CustomerOrder {
    id: ID!
    description: String
}